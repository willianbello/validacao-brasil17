import { SoLetrasComEspacoDirective } from './so-letras-com-espaco.directive';

describe('SoLetrasComEspacoDirective', () => {
  it('should create an instance', () => {
    const directive = new SoLetrasComEspacoDirective();
    expect(directive).toBeTruthy();
  });
});
