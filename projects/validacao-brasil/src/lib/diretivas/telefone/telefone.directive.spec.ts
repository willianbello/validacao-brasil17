import { TelefoneDirective } from './telefone.directive';

describe('TelefoneDirective', () => {
  it('should create an instance', () => {
    const directive = new TelefoneDirective();
    expect(directive).toBeTruthy();
  });
});
