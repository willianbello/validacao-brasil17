import { TituloEleitorDirective } from './titulo-eleitor.directive';

describe('TituloEleitorDirective', () => {
  it('should create an instance', () => {
    const directive = new TituloEleitorDirective();
    expect(directive).toBeTruthy();
  });
});
