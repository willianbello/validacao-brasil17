import { CelularDirective } from './celular.directive';

describe('CelularDirective', () => {
  it('should create an instance', () => {
    const directive = new CelularDirective();
    expect(directive).toBeTruthy();
  });
});
