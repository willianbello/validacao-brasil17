import { SoNumeroComEspacoDirective } from './so-numero-com-espaco.directive';

describe('SoNumeroComEspacoDirective', () => {
  it('should create an instance', () => {
    const directive = new SoNumeroComEspacoDirective();
    expect(directive).toBeTruthy();
  });
});
