import { SoNumeroSemEspacoDirective } from './so-numero-sem-espaco.directive';

describe('SoNumeroSemEspacoDirective', () => {
  it('should create an instance', () => {
    const directive = new SoNumeroSemEspacoDirective();
    expect(directive).toBeTruthy();
  });
});
