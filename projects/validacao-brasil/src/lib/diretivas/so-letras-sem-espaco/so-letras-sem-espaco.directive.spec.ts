import { SoLetrasSemEspacoDirective } from './so-letras-sem-espaco.directive';

describe('SoLetrasSemEspacoDirective', () => {
  it('should create an instance', () => {
    const directive = new SoLetrasSemEspacoDirective();
    expect(directive).toBeTruthy();
  });
});
